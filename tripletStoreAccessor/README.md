Tripletstore Accessor
============

The Tripletstore Accessor provides the interfaces for the interaction with the repository, currently managed by a FUSEKI server.

Installation
------------

    mvn clean install